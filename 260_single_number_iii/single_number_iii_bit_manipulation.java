class Solution {
    public int[] singleNumber(int[] nums) {
        int a(0);
        for (int num : nums) 
            a ^= num;
        
        // TODO (WanMok) Use bit manipulation approach to solve
        return (ints);
    }
}